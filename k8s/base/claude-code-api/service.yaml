apiVersion: v1
kind: Service
metadata:
  name: claude-code-api
  labels:
    app: claude-code-api
spec:
  selector:
    app: claude-code-api
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP
